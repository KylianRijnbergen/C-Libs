#include <stdio.h>
#include "cmdUtils.h"

int main(int argc, char* argv);

int main(int argc, char* argv) {
    int len, count;

    len = 100; 
    count = 5;
    print_line(len, count);
    return 0;
}